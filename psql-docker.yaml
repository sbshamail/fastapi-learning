version: "3.9"

services:
  postgres:
    image: postgres:17.6
    container_name: fastapi_postgres
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"
    volumes:
      - /mnt/sda1/hamail/fastapi/practice_projectjs/psql/data:/var/lib/postgresql/data
  
  # postgresql://user:123@localhost:5432/mydb


